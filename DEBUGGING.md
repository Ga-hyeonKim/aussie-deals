# Debugging Log — AussieDeals

실제로 막혔던 문제와 그걸 어떻게 좁혀 나갔는지의 기록.

`SESSION_NOTES.md`는 "언제 무슨 일이 있었나"(시간축), 이 파일은 **"이 증상은 왜 났고
어떻게 확인했나"**(문제축). 3개월 뒤 비슷한 게 터졌을 때 날짜를 기억할 필요 없이
증상으로 찾을 수 있어야 함.

## 쓰는 규칙

- **숫자 없는 케이스는 쓰지 않는다.** "고쳤다"가 아니라 "무엇이 몇 건 틀렸는지 어떻게
  셌는지"가 이 파일의 값. 추측으로 고친 건 여기 올 자격이 없음
- 실패한 가설도 남긴다 — 어디를 잘못 봤는지가 다음번에 시간을 아껴줌
- `**배운 것**`은 직접 쓴다. 사실은 채워져 있어도 해석은 본인 것이어야 함

---

## 2026-08-11 · 프로덕션 빌드만 실패, 로컬은 정상

**증상** — Vercel에서 빌드 실패 메일이 반복. 그런데 배포된 사이트는 멀쩡히 떠 있음.
`npm run dev`는 아무 문제 없음.

```
⨯ useSearchParams() should be wrapped in a suspense boundary at page "/favorites"
Error occurred prerendering page "/favorites"
```

**좁혀간 과정**
1. "사이트가 안 터졌는데?" → 빌드 실패 시 Vercel은 해당 배포를 **승격하지 않고 버림**.
   직전 성공 배포가 계속 서빙됨. 터진 게 아니라 **업데이트가 멈춘** 상태
2. 커밋 시각 대조 → 마지막 성공 배포는 `28d5829`(08-10 13:06). 그 뒤 22시간 작업이
   전부 라이브에 없었음
3. `useSearchParams`를 쓰는 곳 전수 조사 → `DealsGrid`(`app/page.tsx:48`)와
   `SearchBar`(`app/search/page.tsx:31`)는 이미 `<Suspense>`로 감싸져 있고
   `app/favorites/page.tsx`만 누락

**원인** — `/favorites`는 파일 전체가 `"use client"`이고 라우트 레벨 동적 데이터가 없어서
Next가 빌드 타임 정적 프리렌더를 시도함. 프리렌더 시점엔 쿼리스트링이 존재하지 않으므로
(`/favorites`와 `/favorites?q=twix`가 같은 정적 HTML 하나여야 하는데 불가능) CSR로 bail out
하는데, 경계를 알려주는 `<Suspense>`가 없으면 페이지 전체를 포기해야 해서 에러가 됨.

**왜 dev에서 안 잡혔나** — dev는 매 요청마다 실제 URL이 있는 상태로 렌더하므로
프리렌더 자체를 하지 않음. **프로덕션 빌드 전용 에러.**

**처방** — `components/FavoritesClient.tsx`로 본문 분리, 라우트는 서버 컴포넌트로
축소해 `<Suspense>`로 감쌈 (`8f259c1`)

**재발 방지** — URL 파라미터나 `useSearchParams`를 건드리는 커밋 전에 `npm run build`
한 번. 20초면 끝나고 Vercel 이메일 왕복보다 빠름.

**배운 것** —

---

## 2026-08-11 · 세일 아닌데 ON SALE 뱃지

**증상** — Cetaphil Baby Lotion 상세 페이지에서 콜스가 `$15.80 ON SALE`. 그런데 울리스도
$15.80이고, 매장에서 실물 확인한 결과 할인 중이 아님.

**좁혀간 과정**
1. DB 직접 조회 → 콜스에 라이브 특가 행이 실제로 존재:
   `sale=$15.80 orig=null pct=null 2026-08-04~08-11`
   카탈로그 가격도 $15.80. **할인폭이 0원인데 특가 테이블에 있음**
2. 범위 측정 → 매장별로 갈림:
   ```
   WOOLWORTHS: 5,467 live,     0 fake (0.0%)
   COLES:      6,619 live, 1,985 fake (30.0%)
   ```
   울리스가 0건이라는 게 결정적. 공용 코드가 아니라 **콜스 스크래퍼 문제**로 좁혀짐
3. 만료일 확인 → 1,985건 전부 `validTo = 08-11` 한 묶음. 그날 밤 자동 소멸.
   **정리 스크립트 불필요**, 대신 다음 수요일 크론이 새 배치를 넣는 게 문제

**원인 (둘)**
- 스크래퍼: `fetch-coles.ts`가 `/on-special`에 뜬 걸 전부 `Product`에 저장. 콜스는
  was-price 없는 항목("Low Price", "New", 멀티바이)도 그 페이지에 올림
- UI: 6곳이 각자 "`currentDeal`이 존재하면 세일"로 판단. **실제로 싸졌는지는 아무도 안 봄**

**처방**
- `originalPrice`가 있고 `salePrice < originalPrice`일 때만 `Product`에 기록 (`16bd19d`)
- `lib/deal.ts`의 `isRealDeal()`로 판단을 한 곳에 모으고 6곳 교체 (`a5f8c52`)
- 걸러진 건수를 `promotionType`별로 로그에 집계 — 멀티바이가 많으면 제대로 모델링할 근거

**판단 하나** — 저 1,985건 중 일부가 진짜 "2 for $5"일 수 있지만, 현재 UI엔
(`salePrice`/`originalPrice`/`%`뿐) 그걸 표현할 방법이 없음. 그대로 두면 정가에
ON SALE로 렌더되므로 **버리는 쪽이 어느 경우든 덜 틀림.**

**배운 것** —

---

## 2026-08-11 · 같은 페이지에서 카드와 차트의 가격이 다름

**증상** — 상품 카드는 양쪽 매장 `$15.80`. 바로 아래 차트 판정 배너는
"Cheapest at Woolworths right now · $9.40 · $6.40 less than Coles".

**좁혀간 과정**
1. 두 값의 출처가 다름을 확인
   - 카드 = `StoreProduct.price` + 라이브 특가 → 진짜 현재가
   - 차트 = **PriceHistory의 마지막 행**을 "지금 가격"으로 간주
2. 해당 상품 이력 조회:
   ```
   울리스 마지막 기록: 2026-07-28  $9.40   ← 2주 전, 그 주 세일가
   콜스   마지막 기록: 2026-08-04  $15.80
   ```
   **서로 다른 날짜를 비교**하고 있었음
3. 예외인지 확인 → 예외가 아니었음:
   ```
   양쪽 매장 이력이 있는 ProductGroup:       3,314개
   두 시계열의 끝 날짜가 다른 것:            3,225개 (97.3%)
   ```
   크로스스토어 판정이 사실상 전부 신뢰 불가

**곁가지** — Tim Tam도 틀려 있었으나 카드가 맞아서 눈에 안 띄었음. 차트가 울리스를
08-03 기록($6.40)에서 읽어 "양쪽 같은 가격"이라고 했음. **증상이 안 보인다고 정상이
아니다**의 사례.

**원인** — PriceHistory는 구멍 난 아카이브(스크래퍼가 매주 모든 상품을 잡지 못함)인데
차트가 그 꼬리를 "현재"의 대리값으로 사용. 매장별로 꼬리가 끊긴 날짜가 다르므로
stale vs fresh 비교가 됨.

**처방** — 페이지가 카드 렌더에 이미 계산해둔 `effectivePrice`를 차트에 prop으로 주입.
차트는 오늘 점을 그 값으로 고정 → 양쪽 시계열이 같은 날 끝나고, 배너가 카드와
어긋날 수 **없게** 됨. PriceHistory는 "과거의 모양"으로 강등 (`e5c28d7`)

**설계 원칙** — 같은 숫자를 두 곳에서 각자 유도하면 언젠가 갈라짐. 한쪽이 계산하고
다른 쪽은 받아쓰게 만들면 불일치가 구조적으로 불가능해짐.

**배운 것** —

---

## 2026-08-11 · 용량이 다른 상품이 같은 그룹으로 매칭 (진행 중)

**증상** — Cetaphil Daily Advance Lotion 크로스스토어 페이지에서 울리스 473mL $27와
콜스 226g $20을 나란히 비교. 이름은 동일하지만 다른 제품.

**좁혀간 과정**
1. Cetaphil 그룹 전수 조회 → 8개 중 3개가 용량 불일치 (226g↔473mL, 236mL↔1L 두 건)
2. 문자열 단순 비교로 전체 측정 → 3,362개 중 740개(22%). 다만 `"1L"` vs `"1000mL"`처럼
   **표기만 다른 것이 섞인 상한선**
3. 단위 파서 프로토타입을 써서 재측정 → 실제 오류율:
   ```
   용량 일치:     2,612 (77.7%)
   용량 불일치:     546 (16.2%)   ← 진짜 오류
   파싱 불가:       204 (6.1%)    ← "each", 빈 값, "375mL x 10 pack"
   ```
4. 심한 사례: `170g ↔ 1kg` (Jalna 요거트), `160g ↔ 900g` (Chobani)

**원인** — `match-products.ts`가 `canonical_brand` 일치 + `normalizedName` 임베딩
유사도만 사용. **`unit`을 전혀 보지 않음.** 게다가 `normalizedName`이 용량을 제거하므로
226g와 473mL이 똑같이 `"daily advance lotion"`이 됨 — 임베딩에 구별할 정보가 없었음.
브랜드는 하드 게이트를 걸었는데 용량은 게이트가 없는 비대칭.

**중요한 관찰** — 울리스도 226g/473mL 둘 다 있고 콜스도 둘 다 있음. **정답 짝이 데이터에
존재하는데** 유사도 순 greedy 배정이 엉뚱하게 짝지음. 용량 게이트는 틀린 매칭을 없앨
뿐 아니라 **맞는 매칭을 만들어냄.**

**처방 (계획)**
1. `lib/unit.ts` — `parseSize()` / `sameSize()`
2. `match-products.ts`에 게이트: 용량이 다르면 거부, 파싱 불가면 통과
3. 기존 546개 해체 후 재매칭

**배운 것** —
