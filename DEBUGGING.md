# Debugging Log — AussieDeals

실제로 막혔던 문제와 그걸 어떻게 좁혀 나갔는지의 기록.

`SESSION_NOTES.md`는 "언제 무슨 일이 있었나"(시간축), 이 파일은 **"이 증상은 왜 났고
어떻게 확인했나"**(문제축). 3개월 뒤 비슷한 게 터졌을 때 날짜를 기억할 필요 없이
증상으로 찾을 수 있어야 함.

## 쓰는 규칙

- **숫자 없는 케이스는 쓰지 않는다.** "고쳤다"가 아니라 "무엇이 몇 건 틀렸는지 어떻게
  셌는지"가 이 파일의 값. 추측으로 고친 건 여기 올 자격이 없음
- 실패한 가설도 남긴다 — 어디를 잘못 봤는지가 다음번에 시간을 아껴줌
- `**배운 것**`은 **면접에서 답할 수 있는 형태**로 쓴다. 측정이 실제로 뒷받침하는
  주장, 그때 내린 판단, 이 버그를 넘어 일반화되는 것 — 셋. 고친 내용 반복이나
  자화자찬은 빼고. (2026-08-12부터 Claude가 작성)

---

## 2026-08-11 · 프로덕션 빌드만 실패, 로컬은 정상

**증상** — Vercel에서 빌드 실패 메일이 반복. 그런데 배포된 사이트는 멀쩡히 떠 있음.
`npm run dev`는 아무 문제 없음.

```
⨯ useSearchParams() should be wrapped in a suspense boundary at page "/favorites"
Error occurred prerendering page "/favorites"
```

**좁혀간 과정**

1. "사이트가 안 터졌는데?" → 빌드 실패 시 Vercel은 해당 배포를 **승격하지 않고 버림**.
   직전 성공 배포가 계속 서빙됨. 터진 게 아니라 **업데이트가 멈춘** 상태
2. 커밋 시각 대조 → 마지막 성공 배포는 `28d5829`(08-10 13:06). 그 뒤 22시간 작업이
   전부 라이브에 없었음
3. `useSearchParams`를 쓰는 곳 전수 조사 → `DealsGrid`(`app/page.tsx:48`)와
   `SearchBar`(`app/search/page.tsx:31`)는 이미 `<Suspense>`로 감싸져 있고
   `app/favorites/page.tsx`만 누락

**원인** — `/favorites`는 파일 전체가 `"use client"`이고 라우트 레벨 동적 데이터가 없어서
Next가 빌드 타임 정적 프리렌더를 시도함. 프리렌더 시점엔 쿼리스트링이 존재하지 않으므로
(`/favorites`와 `/favorites?q=twix`가 같은 정적 HTML 하나여야 하는데 불가능) CSR로 bail out
하는데, 경계를 알려주는 `<Suspense>`가 없으면 페이지 전체를 포기해야 해서 에러가 됨.

**왜 dev에서 안 잡혔나** — dev는 매 요청마다 실제 URL이 있는 상태로 렌더하므로
프리렌더 자체를 하지 않음. **프로덕션 빌드 전용 에러.**

**처방** — `components/FavoritesClient.tsx`로 본문 분리, 라우트는 서버 컴포넌트로
축소해 `<Suspense>`로 감쌈 (`8f259c1`)

**재발 방지** — URL 파라미터나 `useSearchParams`를 건드리는 커밋 전에 `npm run build`
한 번. 20초면 끝나고 Vercel 이메일 왕복보다 빠름.

**배운 것** — dev에서 통과한 건 "작동한다"가 아니라 **"이 실행 모드에서 작동한다"**다.
프로덕션 빌드는 다른 모드이고, 다른 모드는 따로 확인해야 한다.
그리고 이 버그가 22시간 살아있었던 이유는 **빌드 실패가 사이트를 망가뜨리지 않기**
때문이다. 직전 배포가 계속 서빙되니까 화면상으로는 아무 일도 안 일어난다.
조용한 실패가 가장 오래 산다 — "안 터졌다"를 "잘 됐다"로 읽지 말 것.

---

## 2026-08-11 · 세일 아닌데 ON SALE 뱃지

**증상** — Cetaphil Baby Lotion 상세 페이지에서 콜스가 `$15.80 ON SALE`. 그런데 울리스도
$15.80이고, 매장에서 실물 확인한 결과 할인 중이 아님.

**좁혀간 과정**

1. DB 직접 조회 → 콜스에 라이브 특가 행이 실제로 존재:
   `sale=$15.80 orig=null pct=null 2026-08-04~08-11`
   카탈로그 가격도 $15.80. **할인폭이 0원인데 특가 테이블에 있음**
2. 범위 측정 → 매장별로 갈림:
   
   ```
   WOOLWORTHS: 5,467 live,     0 fake (0.0%)
   COLES:      6,619 live, 1,985 fake (30.0%)
   ```
   
   울리스가 0건이라는 게 결정적. 공용 코드가 아니라 **콜스 스크래퍼 문제**로 좁혀짐
3. 만료일 확인 → 1,985건 전부 `validTo = 08-11` 한 묶음. 그날 밤 자동 소멸.
   **정리 스크립트 불필요**, 대신 다음 수요일 크론이 새 배치를 넣는 게 문제

**원인 (둘)**

- 스크래퍼: `fetch-coles.ts`가 `/on-special`에 뜬 걸 전부 `Product`에 저장. 콜스는
  was-price 없는 항목("Low Price", "New", 멀티바이)도 그 페이지에 올림
- UI: 6곳이 각자 "`currentDeal`이 존재하면 세일"로 판단. **실제로 싸졌는지는 아무도 안 봄**

**처방**

- `originalPrice`가 있고 `salePrice < originalPrice`일 때만 `Product`에 기록 (`16bd19d`)
- `lib/deal.ts`의 `isRealDeal()`로 판단을 한 곳에 모으고 6곳 교체 (`a5f8c52`)
- 걸러진 건수를 `promotionType`별로 로그에 집계 — 멀티바이가 많으면 제대로 모델링할 근거

**판단 하나** — 저 1,985건 중 일부가 진짜 "2 for $5"일 수 있지만, 현재 UI엔
(`salePrice`/`originalPrice`/`%`뿐) 그걸 표현할 방법이 없음. 그대로 두면 정가에
ON SALE로 렌더되므로 **버리는 쪽이 어느 경우든 덜 틀림.**

**배운 것** — "X가 있으면 Y다"라고 코드에 쓸 때, **X를 만드는 쪽이 Y를 보장하는지**
확인한 적이 없으면 그건 검증된 사실이 아니라 가정이다. 특히 X를 만드는 쪽이 남의
시스템(콜스)이면 보장은 없다.
그리고 범인을 3분에 찾은 건 **비대칭** 덕이었다. 울리스 0% / 콜스 30%가 나온 순간
공용 코드는 용의자에서 빠졌다. 두 경로가 같아야 하는데 다르면, 그 차이가 범위를 좁혀준다.

---

## 2026-08-11 · 같은 페이지에서 카드와 차트의 가격이 다름

**증상** — 상품 카드는 양쪽 매장 `$15.80`. 바로 아래 차트 판정 배너는
"Cheapest at Woolworths right now · $9.40 · $6.40 less than Coles".

**좁혀간 과정**

1. 두 값의 출처가 다름을 확인
   - 카드 = `StoreProduct.price` + 라이브 특가 → 진짜 현재가
   - 차트 = **PriceHistory의 마지막 행**을 "지금 가격"으로 간주
2. 해당 상품 이력 조회:
   
   ```
   울리스 마지막 기록: 2026-07-28  $9.40   ← 2주 전, 그 주 세일가
   콜스   마지막 기록: 2026-08-04  $15.80
   ```
   
   **서로 다른 날짜를 비교**하고 있었음
3. 예외인지 확인 → 예외가 아니었음:
   
   ```
   양쪽 매장 이력이 있는 ProductGroup:       3,314개
   두 시계열의 끝 날짜가 다른 것:            3,225개 (97.3%)
   ```
   
   크로스스토어 판정이 사실상 전부 신뢰 불가

**곁가지** — Tim Tam도 틀려 있었으나 카드가 맞아서 눈에 안 띄었음. 차트가 울리스를
08-03 기록($6.40)에서 읽어 "양쪽 같은 가격"이라고 했음. **증상이 안 보인다고 정상이
아니다**의 사례.

**원인** — PriceHistory는 구멍 난 아카이브(스크래퍼가 매주 모든 상품을 잡지 못함)인데
차트가 그 꼬리를 "현재"의 대리값으로 사용. 매장별로 꼬리가 끊긴 날짜가 다르므로
stale vs fresh 비교가 됨.

**처방** — 페이지가 카드 렌더에 이미 계산해둔 `effectivePrice`를 차트에 prop으로 주입.
차트는 오늘 점을 그 값으로 고정 → 양쪽 시계열이 같은 날 끝나고, 배너가 카드와
어긋날 수 **없게** 됨. PriceHistory는 "과거의 모양"으로 강등 (`e5c28d7`)

**설계 원칙** — 같은 숫자를 두 곳에서 각자 유도하면 언젠가 갈라짐. 한쪽이 계산하고
다른 쪽은 받아쓰게 만들면 불일치가 구조적으로 불가능해짐.

**배운 것** — 같은 값을 두 곳에서 각자 유도하면 언젠가 갈라진다. 규칙을 맞추는 걸로는
안 되고 **한쪽이 계산하고 다른 쪽은 받아쓰게** 해야 갈라질 방법이 없어진다.
그리고 Tim Tam이 무서웠다. 거기도 틀려 있었는데 **카드가 맞아서 눈에 안 띄었다.**
증상이 안 보이는 것과 정상인 것은 다르다. 하나를 고칠 때 같은 원인의 다른 사례를
전수로 세봐야 하는 이유가 이거다.

---

## 2026-08-11 · Neon 512MB 한도 — 그리고 임베딩의 63%가 애초에 무용지물

**증상** — 임베딩 재생성이 첫 배치에서 죽음. 0개 저장.

```
could not extend file because project size limit (512 MB) has been exceeded
```

**좁혀간 과정**

1. 테이블별 크기 측정:
   
   ```
   price_history    250 MB  (테이블 114 + 인덱스 136)   1,095,792행
   store_products   154 MB  (테이블 138 + 인덱스  16)      97,571행
   Product           77 MB                             174,587행
   총 490 MB / 512 MB
   ```
2. `store_products`가 97,571행에 138MB = **행당 1,481바이트**. 이름·가격 몇 개인
   테이블이 그럴 수 없음 → 뭔가 남아있다고 판단
3. **첫 가설: 죽은 튜플. 틀렸음.** `n_dead_tup`이 2,811 / 97,571 = 2.9%뿐
4. 재측정 — `pg_stats`의 `avg_width` 합으로 실제 필요량을 계산:
   
   ```
   bytes/row 실제:  1,481
   bytes/row 예상:  ~309
   설명 안 되는 것: 1,172 bytes/row = 109 MB
   vector(256):     ~1,028 bytes/row
   attisdropped 컬럼: 2개
   ```
   
   → 죽은 튜플이 아니라 **지워진 컬럼의 바이트가 살아있는 행 안에** 남은 것.
   Postgres는 `DROP COLUMN` 시 테이블을 재작성하지 않음
5. 회수 방법 확인 → 일반 `VACUUM`은 불가(죽은 튜플이 아니므로),
   `VACUUM FULL`은 테이블 크기(138MB)만큼 여유가 필요한데 22MB뿐 → **막힘**
6. 다른 각도로 전환: "공간을 어떻게 늘리나"가 아니라
   **"이 임베딩이 다 필요한가"**를 물음. 매칭 쿼리가
   `w.canonical_brand = c.canonical_brand`로 조인하므로 브랜드가 한쪽 매장에만
   있으면 매칭이 원리적으로 불가능:
   
   ```
   normalized_name 있는 상품:       98,805
   브랜드가 양쪽 매장에 다 있는 것:  36,229 (36.7%)
   저장 공간: 전체 97MB  vs  필요한 것만 36MB
   ```
   
   **62,576개, 61MB가 구조적으로 무용지물이었음**
7. 인덱스 사용량도 측정 → `price_history_pkey` 52MB, `idx_scan = 0`.
   조회는 전부 `(storeProductId, recordedAt)` 복합 인덱스(84MB, 6,972회)로 나감

**원인** — 두 겹. 표면은 저장 한도지만, 실제로는 (a) 지워진 컬럼 공간이 회수되지
않았고 (b) 처음부터 매칭 불가능한 상품까지 임베딩하고 있었음. (b)는 한도와 무관하게
잘못이었고, 한도 때문에 드러났을 뿐.

**처방 (계획)** — TODO Priority 0.5의 H → G → F

1. `price_history`의 PK를 `(storeProductId, recordedAt)` 복합키로 변경 (52MB + 27MB)
2. 임베딩은 양쪽 매장 공통 브랜드 36,229개만 생성 (97MB → 36MB)
3. 그 다음 용량 게이트로 ProductGroup 전체 재생성

**되돌아보면** — 3번에서 가설이 틀렸을 때 "그럼 회수를 어떻게 하지"로 계속 파는 대신
"애초에 이만큼 필요한가"로 질문을 바꾼 게 전환점. 공간을 61MB 늘리는 것보다
61MB를 안 쓰는 게 쉬웠음.

**배운 것** — 공간이 부족할 때 "어떻게 늘리지"는 두 번째 질문이다. 먼저 "이게 다
필요한가"를 물으면 늘릴 필요가 없어질 수 있다. 그리고 답은 이미 코드 안에 있었다 —
매칭 쿼리가 `canonical_brand` 일치를 요구한다는 조건을 **읽을 때만 쓰고 쓸 때는 안
썼다.** 필터 조건이 있으면 저장 단계에도 같은 조건을 적용할 수 있는지 봐야 한다.

---

## 2026-08-11 · 위 처방을 실행 — 계획한 두 가지가 틀렸고, 순서가 답이었다

**증상** — 없음. 위 항목의 처방을 실행하는 단계. 계획은 "PK를 복합키로 바꿔 52MB
인덱스 + 27MB 컬럼 데이터 회수, 그 전에 중복 행 제거"였음.

**좁혀간 과정**

1. 실행 전 재측정에서 계획의 전제 하나가 무너짐:
   
   ```
   (store_product_id, recordedAt) 중복:        0건
   (store_product_id, recordedAt::date) 중복: 103,749건
   ```
   
   중복은 **날짜** 기준이지 **timestamp** 기준이 아니었음. 복합 PK는 timestamp로
   걸리므로 **중복 제거 선행이 애초에 필요 없었음**
2. 두 번째 전제도 틀림 — `DROP COLUMN`으로 27MB를 회수한다고 썼는데, **바로 위
   항목에서 배운 것이 그 반대**였음. Postgres는 `DROP COLUMN` 시 테이블을
   재작성하지 않으므로 바이트는 살아있는 행 안에 남음. 진단에서 알아낸 사실이
   같은 문서의 처방 줄에 반영되지 않았음
3. 중복 103,749행의 정체를 물음 — 워크플로 4개가 주 4회 쌓는 것이라는 기존 추정을
   분포로 확인:
   
   ```
   (상품, 날짜)당 1행:  907,376   ← 정상
              2행:      73,597
              3행 이상:  8,000여
   날짜별 평균: 월요일 1.00, 겹치는 날 1.05~1.30
   ```
   
   주 4회가 아니라 **specials와 catalog가 같은 날 겹칠 때만** 생김. 추정보다 온건
4. `store_products`의 시체를 살아있는 payload와 직접 비교 (`pg_column_size`):
   
   ```
   살아있는 데이터:   72MB (764 bytes/행)
   물리 크기:        138MB (1,463 bytes/행)
   차이:              66MB (699 bytes/행)   ← 드롭된 컬럼 2개
   ```
   
   `attnum` 14·15, 둘 다 가변길이. 스키마 컬럼 순서상 14번이 `embedding`,
   15번은 재생성 후 두 번째로 드롭된 것. **`vector(256)` = 1,032 bytes/행**
5. 반대로 `price_history`는 살아있는 103 vs 물리 109 bytes/행 — 시체가 없음.
   **250MB는 전부 진짜 행.** 두 테이블의 문제가 서로 다른 종류였음
6. 위 항목에서 "`VACUUM FULL`은 138MB 여유가 필요한데 22MB뿐 → 막힘"으로 닫았던
   길을 다시 봄. 인덱스를 먼저 지우면 **그 순간에만** 창이 열림:
   
   ```
   1. DROP price_history_pkey          490 → 438MB
   2. DROP 복합 인덱스                  438 → 354MB   여유 158MB
   3. VACUUM FULL store_products       ← 이 창에서만 가능
   4. ADD PRIMARY KEY (복합)
   ```
   
   순서를 뒤집으면(4를 3보다 먼저) 여유가 82MB로 줄어 3이 실패함
7. 실행 시간이 걱정되어 먼저 측정 → Neon compute 247 MB/s, 1.1M행 정렬 2.2초.
   전체 몇 분이 아니라 **6초**

**원인** — 계획을 세운 시점과 실행 시점 사이에 새 정보가 없었는데도 계획이 틀렸음.
1·2 모두 실행 직전 재측정으로만 드러남. 특히 2는 **같은 문서 안에 정답이 이미 적혀
있었음.**

**처방** — `scripts/reclaim-space.ts` (6단계, 각 단계 전후 크기 로깅, 재실행 가능).
결과:

```
전체:            490MB → 305MB   (여유 22MB → 207MB)
store_products   154MB →  43MB   (VACUUM FULL, -109MB)
price_history    250MB → 176MB   (인덱스 136 → 62MB)
```

`VACUUM FULL`이 예상 66MB가 아닌 109MB를 뱉음 — 죽은 튜플과 인덱스 재작성분이 더
있었음. 새 복합 PK는 예상 76MB가 62MB (`id` 컬럼이 빠진 뒤 정렬된 상태로 빌드).

검증: `prisma db push`가 `already in sync`(= 다음 워크플로 실행이 되돌리지 않음),
차트 쿼리가 새 PK로 0.24ms, 1,095,792행과 98,805개 `normalized_name` 그대로.

**남은 것** — `price_history`의 중복 103,749행(~11MB)과 `id` 시체는 테이블 재작성이
필요. 이제 여유 207MB라 가능해졌지만 급하지 않음 (TODO Priority 5.5).

**배운 것** — 공간이 부족할 때 **무엇을 지울지보다 어떤 순서로 지울지가 답이었다.**
`VACUUM FULL`은 88MB 여유가 필요한데 22MB뿐이라 "불가능"으로 닫아뒀는데, 실제로는
안 쓰는 인덱스 두 개를 먼저 떨어뜨린 직후 158MB가 비는 창이 있었다. 각 단계의
가능/불가능만 보면 못 찾고, **단계 사이의 상태 변화를 봐야 보인다.** 순서를 뒤집어
PK를 먼저 만들면 여유가 82MB로 줄어 3단계가 실패한다 — 같은 작업 집합인데 순서만으로
성패가 갈린다.

두 번째는 더 불편한 것이다. 계획에 적은 전제 두 개가 실행 시점에 둘 다 틀렸고,
**그중 하나는 이 파일 바로 위 항목에 이미 정답이 적혀 있었다**(`DROP COLUMN`은 공간을
회수하지 않는다). 진단을 문서화하는 것과 그 진단이 다음 처방에 반영되는 것은 별개
사건이다. 그래서 실행 직전 재측정을 절차로 넣었다 — 계획과 실행 사이에 새 정보가
없어도 전제는 틀려 있을 수 있다.

셋째, 예상치는 두 번 다 빗나갔다(회수 66MB→109MB, 신규 인덱스 76MB→62MB). 방향은
맞았지만 크기는 아니었으므로, 여유가 빠듯한 작업에서 예상치에 기대 순서를 짜면 안
된다. 단계마다 실제 크기를 찍게 스크립트를 만든 게 그래서였다.

---

## 2026-08-12 · 크론은 초록불, `price_history`는 한 주가 통째로 비어 있었다

**증상** — 없음. 위 작업을 커밋하기 전에 "다음 크론이 이걸 되돌리나" 확인하려고
Actions 로그를 열었다가 발견. 아무도 신고하지 않은 고장이었다.

**좁혀간 과정**

1. 08-12 새벽 크론이 `success`인데 `price_history`의 마지막 날짜가 08-10:
   
   ```
   Product         08-11 배치 정상 (Coles 5,259 / Woolworths 2,941)
   store_products  08-11 갱신 정상 (98,815행)
   price_history   08-10이 마지막   ← 08-11 주가 없음
   ```
2. 로그에서 `PriceHistory createMany 실패`가 **143번** 반복. 원인은 전부 하나:
   `The column 'id' does not exist in the current database.`
3. 그 위를 보니 `db push`가 3번 다 실패해 있었음:
   
   ```
   The required column `id` was added to the `price_history` table with a
   prisma-level default value. There are 1095792 rows in this table, it is
   not possible to execute this step.
   ```
4. 그런데도 다음 스텝이 실행됨. 워크플로를 보니:
   
   ```bash
   for i in 1 2 3; do
     npx prisma db push --accept-data-loss && break
     echo "Attempt $i failed, waiting 10s..."
     sleep 10          # ← 3번 다 실패하면 마지막 명령이 sleep = exit 0
   done
   ```
   
   **4개 워크플로 전부** 같은 모양
5. 스크립트도 종료 코드를 안 냄 — `.catch(e => console.error(...))`가 **14곳**
6. 마지막 한 겹: 완료 로그가 `products.length`(시도한 수)를 출력.
   `saved += batch.length`는 진행률 표시용이라 실패와 무관.
   **실제 성공 건수를 세는 스크래퍼는 4개 중 `fetch-woolworths.ts` 하나뿐**
   → 그래서 로그에 `DB 저장 완료: 7,134개`가 찍히고 실제 저장은 0건
7. 왜 아무 검증도 이걸 못 잡았나 확인:
   
   ```
   tsconfig.json  exclude: ["node_modules", "scripts"]   ← 타입체크 제외
   package.json   typecheck / test 스크립트 없음
   workflows      lint / typecheck / build 스텝 없음
   ```
   
   프로덕션 DB에 쓰는 코드의 유일한 검증이 "크론이 실제로 도는 것"인데,
   1~6 때문에 그 결과가 안 보임

**원인** — 단일 버그가 아니라 **세 겹이 전부 실패를 통과시킴**: 워크플로가 실패를
삼키고(exit 0), 스크립트가 실패를 삼키고(`.catch`), 로그가 시도 수를 성공으로
보고함. 어느 한 겹만 있었어도 잡혔다.

**처방** — (`890c569` 다음 커밋)

1. 워크플로 4개: `&& break` → `if ...; then exit 0; fi` + 마지막에 `exit 1`.
   스키마가 안 맞으면 **쓰지 않는다**
2. `lib/scrape-report.ts` — 종류별 시도/성공/실패를 세고, 판정 두 가지로 종료 코드
   결정: **성공 0건이면 실패**(이 사건), **실패율 5% 초과면 실패**(부분 붕괴).
   같은 에러는 메시지로 묶어서 상위 3개만 출력 (143줄 → 1줄)
3. 스크래퍼 4개 연결. 자체 테스트 18/18 (`scripts/test-scrape-report.ts`)

**남은 것** — 이번에 안 건드린 두 축. TODO Priority 0.7:
스크랩 후 sanity check(축 4), `scripts/` 타입체크 편입(축 2),
`db push` → `prisma migrate` 전환(축 3).

**아직 설명 안 된 것** — 크론 시점(08-12 06:19)에 `id` 컬럼이 없었다는 `db push`의
판단이 몇 시간 뒤 내 측정과 안 맞는다. 내가 `DROP COLUMN`을 실행했을 때 실제로
지워졌으니(`attisdropped` 시체가 그때 생김) 그 시점엔 분명 있었다. 스키마를 맞춰
푸시하면 증상은 사라지지만 원인은 미해결.

**배운 것** — **실패를 세지 않으면 실패는 일어나지 않은 것과 같다.** 이 사건에서
고장 자체는 정상적인 것이었다 — 스키마가 어긋났고 쓰기가 거부됐다. 비정상은 그게
`success`로 보고된 것이고, 원인은 세 겹 어디에도 **개수를 세는 코드가 없었다는**
점이다. 로그는 143줄이나 있었지만 로그는 카운터가 아니다. 사람이 읽어야만 정보가
되는 것은 자동화된 파이프라인에서 정보가 아니다.

특히 완료 로그가 `products.length`(시도한 수)를 출력하고 있었다는 게 핵심이다.
성공률 100%와 0%가 **글자 그대로 같은 문자열**을 냈다. 지표를 고를 때 "실패해도
값이 안 변하는 수치"는 지표가 아니다 — 4개 스크래퍼 중 3개가 그랬다.

방어를 세 겹 쌓는 것과 세 겹이 각자 실패를 통과시키는 것은 다르다. `&& break`,
`.catch(console.error)`, 시도 수 로그 — 각각은 합리적인 선택이었다(재시도는 Neon
콜드 스타트 대응, catch는 상품 하나 때문에 7천 개를 버리지 않으려는 것). **개별로
옳은 결정이 겹치면서 실패가 새는 경로를 만들었다.** 그래서 처방도 catch를 없애는 게
아니라 catch가 개수를 세게 한 것이다.

마지막으로, 이 고장은 **찾으러 간 게 아니라 다른 일을 하다 마주쳤다.** 커밋 전에
"다음 크론이 이걸 되돌리나" 확인하려고 로그를 연 게 전부다. 감시 장치가 없는 시스템에서
고장 발견은 운이고, 운은 규모가 커질수록 나빠진다.

---

## 2026-08-12 · 앱도 같은 병이었다 — 실패가 "데이터 없음"으로 보이던 6곳

**증상** — 없음. 위 사건을 고친 뒤 "앱 쪽에도 같은 게 있나" 물어서 찾은 것.

**좁혀간 과정**

1. 에러 화면 파일을 셈 → `error.tsx` / `global-error.tsx` / `not-found.tsx` /
   `loading.tsx` **0개**. 그런데 페이지 5개 전부가 서버 컴포넌트에서 `prisma`를
   직접 부르고 `try/catch`가 없음
   → Neon 콜드 스타트 한 번에 전 페이지가 `Application error: a server-side
     exception has occurred`. **재시도 버튼도 설명도 없음**
2. 클라이언트 요청을 훑음 → `.then(r => r.ok ? r.json() : [])`가 **4곳**.
   500을 빈 배열로 바꾸고 있었음. 상태가 `loading`/`empty` 둘뿐이라 **실패가 갈 곳이
   없어서 `empty`로 떨어짐**:
   
   ```
   PriceHistoryChart      "No price history yet"      (기록이 몇 달치 있는데도)
   CrossStorePriceChart   같은 문구
   FavoritesClient        찜 목록이 텅 빔             ← 찜한 게 날아간 것처럼 보임
   ```
3. API 라우트 `try/catch` 커버리지 → 핸들러 6개 중 `favorites` 하나만 있음
4. 낙관적 업데이트 3곳이 응답을 안 봄 — 하트 토글(`useFavorites`), 찜 삭제 2곳.
   실패해도 UI는 성공한 척하고 새로고침하면 되살아남
5. **서버가 실패를 성공으로 위장하는 것도 발견** — `DELETE /api/favorites`가
   `.catch(() => {})`로 모든 에러를 삼키고 `{ok:true}` 반환.
   4번을 고쳐도 **클라이언트가 실패를 알 방법이 없음**

**부수 발견 (측정으로만 잡힘)** — `loading.tsx`를 넣자 `notFound()`가 **404 대신
200**을 반환하기 시작. 상태 코드를 실제로 재보지 않았으면 못 봤을 것:

```
loading.tsx 있음:  /product/<없는id>  → HTTP 200   (3개 라우트 전부)
loading.tsx 없음:  /product/<없는id>  → HTTP 404
```

원인: `loading.tsx`가 있으면 Next가 즉시 스트리밍을 시작 → 200 헤더가 이미 나간
뒤에 "없는 상품"임을 알게 됨 → 늦어서 못 바꿈.
해결: 메인만 `(browse)` 라우트 그룹으로 옮겨 로딩 UI 범위를 좁힘. 괄호 폴더는
URL에 안 나타나므로 주소는 `/` 그대로, 상세 라우트는 스트리밍 안 해서 404 회복

**원인** — 파이프라인 사건과 **같은 병의 다른 환자**. 실패를 정상으로 위장함.
다만 속는 대상이 다르다: 그쪽은 개발자(초록불), 이쪽은 사용자(정상 화면).

**처방** — (`fbb8c3f`, `89b8682`) 에러 화면 4종 + 실패 상태 분리 6곳 + 롤백 3곳.
서버가 `P2025`(이미 없음)만 정상으로 치고 나머지는 500으로 올림.
검색 취소(`AbortError`)는 실패로 세지 않음 — 한 글자 더 치면 나는 정상 동작

**검증** — DB는 살리고 API만 500으로 막아 브라우저에서 4개 화면 확인.
`curl`로는 불가능: 클라이언트 fetch 실패는 렌더 후 콜백에서 나므로 경계가 못 잡고
HTML도 동일함. `error.tsx`도 마찬가지로 빈 셸만 오고 화면은 브라우저에서 그려짐

**배운 것** — **"에러 처리"와 "실패를 사용자에게 정직하게 보여주기"는 다른 일이다.**
6곳 모두 에러를 잡고 있었다. `.catch`도 있고 `r.ok` 검사도 있었다. 잡은 다음 그것을
**빈 값으로 바꿔서** 정상 경로로 흘려보낸 게 문제였다. 코드 리뷰에서 `.catch`가
있는지 세는 것으로는 절대 못 잡는 종류다 — 잡은 뒤 무엇으로 바꾸는지를 봐야 한다.

상태가 부족하면 실패는 가장 가까운 상태로 흡수된다. `loading`/`empty` 두 개뿐인
컴포넌트에서 실패가 `empty`가 된 건 버그라기보다 **모델링 누락**이었다. UI 상태를
셀 때 "이 화면이 구분해야 하는 사실이 몇 가지인가"를 먼저 물었어야 했다.

계층을 고칠 때는 **가장 바깥부터 확인해야 한다.** 클라이언트 롤백을 다 붙이고 나서야
서버가 `{ok:true}`를 준다는 걸 발견했다. 순서가 반대였으면 클라이언트 작업이 처음부터
동작했을 것이다. 아래에서 위로 고치면 위층이 아래층을 무효화하는지 모른 채 끝난다.

그리고 **없던 파일을 추가하는 것도 변경이다.** `loading.tsx`는 순수한 추가였는데
`notFound()`의 상태 코드를 바꿨다. 추가는 안전하다는 직관이 틀렸고, 상태 코드를
실제로 재보지 않았으면 배포까지 갔을 것이다.

---

## 2026-08-11 · 있어야 할 컬럼이 없음 — `db push`가 임베딩을 지우고 있었다

**증상** — 용량 게이트를 검증하려고 매칭 스크립트를 돌렸더니:

```
Raw query failed. Code: 42703. Message: column w.embedding does not exist
```

그런데 이 스크립트는 전날 정상 동작해서 ProductGroup 3,362개를 만들었음.

**좁혀간 과정**

1. 컬럼 목록 조회 → `embedding` 없음. 단 `vector` 확장은 살아있고
   ProductGroup 3,362개도 그대로 → **누가 컬럼만 지웠다**
2. SESSION_NOTES(08-05)에 단서가 있었음:
   *"embedding/pgvector 컬럼은 Prisma가 vector 타입 미지원이라 Neon SQL Editor에서 직접 추가"*
   → 즉 `schema.prisma`에 없는 컬럼
3. 워크플로 확인 → 4개 전부 `npx prisma db push --accept-data-loss` 실행
4. `prisma migrate diff` → `-- This is an empty migration.`
   즉 DB가 현재 스키마와 완전히 일치. 다른 수동 컬럼은 없음(추가 위험 없음)
5. 시점 추정: 매칭이 08-10 13:06에 성공했고 카탈로그 크론은 격주 월요일 = 08-10.
   그 뒤에 워크플로가 돌면서 날아감

**원인** — `db push`는 `schema.prisma`를 유일한 진실로 취급한다. 스키마에 없는 컬럼은
정의상 "지워야 할 드리프트"이고, `--accept-data-loss`가 허가를 준다.
임베딩 98,595개 소실.

그리고 그 플래그의 출처가 `e6fcde1` (2026-04-17)
— *"Allow data loss in db push to drop unused playing_with_neon table"*.
**4월에 쓰레기 테이블 하나 지우려고 넣은 플래그**가 8월에 이것을 지웠다.

**처방** — `schema.prisma`에 `embedding Unsupported("vector(256)")?` 선언 →
`migrate diff`가 `ALTER TABLE ... ADD COLUMN "embedding" vector(256)`를 생성하는 것을
확인한 뒤 `db push`로 컬럼 재생성 (`cca4457`)

**재발 방지** — CLAUDE.md의 Invariants에 기록:

> **스키마 파일 밖에서 만든 것은, 스키마 파일을 소유한 도구가 지운다.**

그리고 더 일반적으로: **도구가 옳은 관행을 불가능하게 만드는 것처럼 보이면,
관행을 포기하기 전에 탈출구를 찾아라.** Prisma가 `vector`를 모른다는 사실이
"스키마 우선" 원칙의 예외를 만들어주지 않았다. `Unsupported()`는 처음부터 있었다.

**배운 것** — 도구의 한계에 부딪혔을 때 "그럼 도구 밖에서 하자"가 첫 결론이면 아직 덜
찾은 상태다. Prisma가 `vector`를 모른다는 건 사실이었지만, 그래서 스키마 우선 원칙을
버려야 한다는 건 사실이 아니었다. 5분 더 검색할 지점이 거기였다.
그리고 `--accept-data-loss`처럼 **한 번의 편의를 위해 끈 안전장치는 만료되지 않는다.**
4월의 나는 테이블 하나를 지우려고 켰고, 8월까지 켜져 있었다.

---

## 2026-08-11 · 용량이 다른 상품이 같은 그룹으로 매칭

**증상** — Cetaphil Daily Advance Lotion 크로스스토어 페이지에서 울리스 473mL $27와
콜스 226g $20을 나란히 비교. 이름은 동일하지만 다른 제품.

**좁혀간 과정**

1. Cetaphil 그룹 전수 조회 → 8개 중 3개가 용량 불일치 (226g↔473mL, 236mL↔1L 두 건)
2. 문자열 단순 비교로 전체 측정 → 3,362개 중 740개(22%). 다만 `"1L"` vs `"1000mL"`처럼
   **표기만 다른 것이 섞인 상한선**
3. 단위 파서 프로토타입을 써서 재측정 → 실제 오류율:
   
   ```
   용량 일치:     2,612 (77.7%)
   용량 불일치:     546 (16.2%)   ← 진짜 오류
   파싱 불가:       204 (6.1%)    ← "each", 빈 값, "375mL x 10 pack"
   ```
4. 심한 사례: `170g ↔ 1kg` (Jalna 요거트), `160g ↔ 900g` (Chobani)

**원인** — `match-products.ts`가 `canonical_brand` 일치 + `normalizedName` 임베딩
유사도만 사용. **`unit`을 전혀 보지 않음.** 게다가 `normalizedName`이 용량을 제거하므로
226g와 473mL이 똑같이 `"daily advance lotion"`이 됨 — 임베딩에 구별할 정보가 없었음.
브랜드는 하드 게이트를 걸었는데 용량은 게이트가 없는 비대칭.

**중요한 관찰** — 울리스도 226g/473mL 둘 다 있고 콜스도 둘 다 있음. **정답 짝이 데이터에
존재하는데** 유사도 순 greedy 배정이 엉뚱하게 짝지음. 용량 게이트는 틀린 매칭을 없앨
뿐 아니라 **맞는 매칭을 만들어냄.**

**처방** — `lib/unit.ts`의 `parseSize()` / `sameSize()`와 `match-products.ts`의
게이트 (`cca4457`). 용량이 다르면 거부, 용량 주장이 없으면 판단 보류.
파서 자체 테스트 15/15 통과.

팩 구조를 유지한 게 설계 포인트: 총량으로 환산하면 `375mL x 10 pack`이 `3.75L`과
같아지고 `2x100g`가 `200g`과 같아진다. 10캔 묶음은 큰 병이 아니고 트윈팩은 한 통이
아니므로, 환산하면 새로운 오매칭을 만든다.

**아직 실행 안 됨** — 기존 그룹 재생성이 남았고, 그건 임베딩이 필요하고,
임베딩은 Neon 저장 한도에 막혀 있음 (위 항목). TODO Priority 0.5의 F.

**배운 것** — 하드 게이트를 하나 걸었다면 "왜 이것만 걸었나"를 물어야 한다. 브랜드를
게이트로 쓴 이유(다르면 다른 상품이다)가 용량에도 똑같이 적용되는데 용량만 빠져 있었다.
비대칭이 있으면 그 자리가 구멍이다.
그리고 **정규화가 정보를 지운다는 건, 지운 정보로는 판단할 수 없다는 뜻이다.**
`normalizedName`에서 용량을 뺐으면 용량 판단은 다른 경로로 넣어야 했다.

---

# 이전 이력 (커밋 궤적에서 복원)

아래는 이 파일을 만들기 전(2026-04~05)에 해결한 것들을, **git 로그에서 역으로 복원**한
기록. 당시 측정한 숫자는 남아 있지 않으므로 위 항목들처럼 수치가 없음 — 대신
**커밋의 궤적 자체가 증거**다. 몇 번 시도했고 어디서 방향을 틀었는지가 보인다.

---

## 2026-04-28 ~ 04-30 · Imperva 봇 차단 — 4단계에 걸친 우회

프로젝트 전체에서 가장 길었던 문제. 3일, 8커밋.

**증상** — 콜스 데이터를 가져오려는데 JSON 엔드포인트가 HTML(Imperva 챌린지 페이지)을
돌려줌. 브라우저에서는 잘 보이는데 스크립트에서만 막힘.

**시도한 순서 — 각 단계가 다음 층을 드러냄**

1. `64f95ba` 이미지가 안 뜸 → CDN도 Imperva가 막고 있었음. `unoptimized` 처리
2. `2a8d4d1` fetch가 HTML을 받으면 **curl로 재시도**하는 폴백. 부분적으로 통함
3. `ac77261` curl도 결국 막힘 → **Playwright로 전환.** HTTP 레벨 우회를 포기하고
   진짜 브라우저를 쓰기로 방향 전환
4. `d34f40b` 브라우저를 띄워도 페이지 안에서 XHR을 날리면 막힘 →
   **XHR 대신 `page.goto()`로 페이지마다 이동**해서 SSR된 `__NEXT_DATA__`를 읽기
5. `53fb1ae` 세션 중간에 챌린지가 끼어듦 → 3회 재시도 + `waitUntil: "load"`
6. `642acf0` 그래도 탐지됨 → **stealth 플러그인** + 리다이렉트 확정 URL 사용
   (`/on-special` → `/en/on-special` 때문에 page 2부터 깨지고 있었음)
7. `7aec579` 전체 카탈로그 스크래퍼도 같은 방식으로 재작성

**원인** — 봇 차단이 단일 계층이 아님. IP/헤더 → 요청 패턴 → 브라우저 지문 →
세션 중 재검증까지 층이 쌓여 있어서, 한 층을 뚫으면 다음 층이 드러남.

**배운 것** — 막힌 게 한 겹이라고 가정하면 매번 "이번엔 되겠지"로 시간을 쓴다. 상대가
방어를 **의도적으로** 층으로 쌓았다면 내 우회도 층으로 접근해야 한다.
그리고 도구를 갈아타는 결정을 너무 늦게 했다. HTTP로 두 번 실패했을 때 "HTTP를 더 잘
쓰는 방법"(curl 폴백)을 찾은 게 3단계인데, 그때 이미 **문제는 요청 내용이 아니라
요청 주체**였다. 같은 층에서 두 번 실패하면 층을 바꿀 때다.

---

## 2026-04-17 · Prisma 7으로 올린 뒤 CI가 죽음 — 하루 6커밋

**증상** — GitHub Actions에서 `prisma db push` 실패. 로컬에서는 됨.

**시도한 순서**

1. `51fa5e0` `DATABASE_URL`이 없다고 나옴 → datasource에 직접 추가
2. `7ee11c1` **되돌림.** Prisma 7은 `prisma.config.ts`를 쓰는 게 맞음.
   1번은 증상을 가린 잘못된 수정이었음
3. `b6beda0` 추측을 멈추고 **디버그 출력부터 추가**
4. `e0de24b` 진짜 원인 발견 — `--skip-generate` 플래그가 Prisma 7에서 **제거됨**
5. `e6fcde1` 이어서 드러난 별개 문제: 안 쓰는 `playing_with_neon` 테이블 때문에
   push가 막힘 → data loss 허용

**원인** — 메이저 버전 업그레이드가 CLI 계약을 바꿈. 에러 메시지(`DATABASE_URL not
found`)가 진짜 원인(없어진 플래그)과 무관해서 처음 두 시도가 헛돌았음.

**되돌아보면** — 2번에서 되돌리고 3번에서 로그를 찍은 게 전환점. 그 전까지는 에러
메시지를 곧이곧대로 믿고 고치려 했음.

**배운 것** — 에러 메시지는 증상이지 원인이 아니다. "DATABASE_URL이 없다"고 해서
DATABASE_URL 문제가 아니었다.
그리고 **두 번 고쳐서 안 되면 고치기를 멈추고 관측을 늘리는 게 빠르다.** 실제로
3번째 커밋에서 디버그 로그를 찍자마자 4번째에서 풀렸다. 1·2번은 추측이었고 3번은
측정이었다. 하루를 6커밋으로 쓴 이유는 측정을 세 번째로 미뤘기 때문이다.

---

## 2026-04-29 · 콜스 MULTI_SAVE의 `originalPrice = null` (1차)

**증상** — 세일이 끝난 뒤에도 찜 목록에 잘못된 가격이 남음.

**원인** — `StoreProduct.price`를 `p.originalPrice ?? p.salePrice`로 갱신하고 있었는데,
콜스 MULTI_SAVE("2 for $5" 같은) 프로모션은 `originalPrice`가 `null`이라 **세일가가
정가 자리를 덮어씀.**

**처방** — `originalPrice`가 있을 때만 가격 갱신 (`2a8d4d1`)

> **후일담 — 2026-08-11에 같은 사실이 다시 터짐.**
> 이때 발견한 "MULTI_SAVE는 originalPrice가 null"이라는 지식은 `fetch-coles.ts` 한 줄의
> 주석으로만 남았음. 3개월 반 뒤, 같은 사실이 이번엔 **가짜 ON SALE 뱃지**로 돌아옴
> (위 08-11 항목). 몰라서가 아니라 **국소적으로 고치고 이름을 안 붙여서** 그 지식이
> 그 줄에 갇혔기 때문. 이 프로젝트에서 가장 비싼 교훈.

**배운 것** — 알아낸 사실을 주석으로 남기면 **그 줄에서만 산다.** 주석은 그 코드를 읽는
사람에게만 전달되고, 3개월 뒤에 다른 파일을 쓰는 나에게는 전달되지 않는다.
구분 기준이 생겼다: 이번에 알아낸 게 **"이 코드의 사정"이면 주석**, **"데이터의
성질"이면 이름 붙인 함수나 불변식 목록**이다. "MULTI_SAVE는 originalPrice가 null"은
후자였는데 전자로 처리했다.

---

## 2026-05-05 · 울리스 API가 에러 대신 빈 결과를 돌려줌

**증상** — 카탈로그 스크래퍼 속도를 올리려고 `pageSize`를 36 → 100으로 늘렸더니
결과가 0개.

**원인** — 울리스 API가 `pageSize=100`을 **거부하면서 에러가 아니라 빈 배열**을 반환.
성공 응답처럼 보여서 실패가 조용히 지나감.

**처방** — 36으로 되돌림 (`f1428b6`)

**일반화** — 200 OK가 "성공"이 아님. 응답의 *모양*이 아니라 *내용*을 확인해야 함.
오늘(08-11)의 "특가 행이 존재하면 할인"과 같은 종류의 실수 — **관측 가능한 신호를
원하는 의미의 대리값으로 믿은 것.**

**배운 것** — 200 OK와 빈 배열은 실패일 수 있다. **응답이 왔다는 것과 요청이 받아들여
졌다는 것은 다르다.**
그리고 이건 성능 개선이었다는 게 포인트다. "되면 좋고" 성격의 변경은 성공을 확인하지
않고 넘어가기 쉽다 — 속도만 보고 **결과 개수를 비교하지 않았다.** 최적화는 전후의
출력이 같은지부터 봐야 한다.

---

## 2026-04-25 · 네비바 높이 — 5커밋의 시행착오 (반면교사)

**증상** — 로고 PNG의 여백 때문에 네비바가 너무 두꺼움.

**시도** — `049fc9e` z-50 + py 축소 → `a28d2c8` `overflow-hidden` + scale로 PNG 여백
클리핑 → `0362ae1` **클리핑 되돌림** → `49c683e` **잘라낸 이미지로 교체** →
`f8f1637` py 미세조정

**되돌아보면** — 문제는 레이아웃이 아니라 **에셋**이었음. CSS로 4번 우회하다가
결국 이미지를 자르는 게 답이었음. 원인을 특정하지 않고 증상 근처를 건드린 사례.

**일반화** — 두 번 고쳤는데 안 되면, 고치는 걸 멈추고 **어디가 원인인지부터** 확정할 것.

**배운 것** — 같은 증상을 두 번 이상 **서로 다른 방법으로** 고치고 있으면, 그건 내가
원인을 모른다는 신호다. 고치는 중이 아니라 찍는 중인 것.
CSS를 네 번 만진 이유는 "원인은 레이아웃이다"라는 전제를 깔았기 때문이고, 그 전제를
한 번도 검사하지 않았다. 원인이 에셋일 수도 있다는 걸 다섯 번째에 알았다.
작은 문제라서 대충 접근한 거지만, **작은 문제일수록 전제를 안 검사한다.**

---

## 그 외 (한 줄 기록)

- `2ad87e0` (04-28) 홈 화면 ISR 폴백이 과도하게 커짐 → `force-dynamic`으로 전환
- `82e6914` (04-28) PWA 서비스워커가 오래된 페이지를 캐시에서 서빙 → 페이지는
  network-first로 바꾸고 낡은 캐시 제거
- `61724a0` (04-30) 만료된 세일이 장바구니에서 사라짐 → 항목은 유지하되 카운트
  배지만 유효한 딜을 세도록 분리
- `8e0d8b0` (05-05) 카탈로그 스크래퍼가 Neon 커넥션을 고갈시킴 → 동시성 축소
- `31b0b0e` (07-03) Neon 콜드 스타트로 CI의 `db push`가 간헐 실패 → 재시도 로직
- `7339e0f` (04-23) `scripts/`에서 실행하면 `DATABASE_URL`을 못 찾음 → dotenv 경로 명시
- `39e3188` (04-23) `scripts/`가 Next.js 타입체크에 걸림 → tsconfig에서 제외
